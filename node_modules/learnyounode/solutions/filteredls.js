var dir = process.argv[2];
var ext = process.argv[3];
var fs = require('fs')
fs.readdir(dir, function(err, list) {
    for(var i=0; i<list.length; i++) {
        var listExt = list[i].split(".")[1]
        if(listExt == ext) {
            console.log(list[i])
        }
    } 
});